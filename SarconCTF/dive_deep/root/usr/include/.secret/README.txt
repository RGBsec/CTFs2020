encoded.txt was top secret image encoded using a script, I accidently deleted the script and can't retrieve the image now.

However, backup of Script was uploaded somewhere on the internet in 2 parts.

Goal:- Retrieve 2 parts of script, combine them for encoder script that resulted encoded.txt, create a decoder script and retrieve the image.
